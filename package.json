{
  "scripts": {
    "format": "prettier --write '**/*.*js*' --write '**/*.*json*' --write '**/*.*html*'",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --maxWorkers=100% --coverage"
  },
  "type": "module",
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "@wpdas/wave-header": "^1.0.1",
    "fflate": "^0.7.1",
    "jest": "^27.3.1",
    "prettier": "^2.4.1",
    "rollup": "^2.58.3",
    "simple-yenc": "0.0.2",
    "terser": "^5.9.0"
  }
}
